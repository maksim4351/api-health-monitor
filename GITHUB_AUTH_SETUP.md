# 🔐 Настройка доступа к GitHub

## ⚠️ Важно

Для отправки файлов на GitHub через `git push` нужна аутентификация. Есть несколько способов:

---

## 🚀 Способ 1: Personal Access Token (РЕКОМЕНДУЕТСЯ)

Это самый простой способ для начинающих.

### Шаги:

1. **Создайте Personal Access Token на GitHub:**

   - Откройте: https://github.com/settings/tokens
   - Нажмите **"Generate new token"** → **"Generate new token (classic)"**
   - Введите название: `api-monitor-access`
   - Выберите срок действия (например, 90 дней или "No expiration")
   - Отметьте права доступа:
     - ✅ `repo` (полный доступ к репозиториям)
   - Нажмите **"Generate token"**
   - ⚠️ **ВАЖНО:** Скопируйте токен сразу! Он показывается только один раз!
   - Сохраните токен в безопасном месте

2. **Настройте git (если еще не настроено):**

   ```bash
   git config --global user.name "ВашеИмя"
   git config --global user.email "ваш@email.com"
   ```

3. **При выполнении `git push`:**

   - Когда git попросит пароль, введите:
     - **Username:** ваш логин GitHub
     - **Password:** вставьте Personal Access Token (не пароль!)

---

## 🔑 Способ 2: SSH ключи (для продвинутых)

Более безопасный способ, не требует ввода пароля каждый раз.

### Шаги:

1. **Проверьте, есть ли SSH ключ:**

   ```bash
   ls ~/.ssh/id_rsa.pub
   ```

   Если файла нет, создайте ключ:

   ```bash
   ssh-keygen -t ed25519 -C "ваш@email.com"
   ```

   Нажимайте Enter на все вопросы (можно оставить пустым).

2. **Скопируйте публичный ключ:**

   ```bash
   cat ~/.ssh/id_rsa.pub
   ```

   Скопируйте весь вывод (начинается с `ssh-ed25519` или `ssh-rsa`).

3. **Добавьте ключ на GitHub:**

   - Откройте: https://github.com/settings/keys
   - Нажмите **"New SSH key"**
   - Введите название: `Мой компьютер`
   - Вставьте скопированный ключ
   - Нажмите **"Add SSH key"**

4. **Измените URL репозитория на SSH:**

   ```bash
   git remote set-url origin git@github.com:maksim4351/api-health-monitor.git
   ```

5. **Проверьте подключение:**

   ```bash
   ssh -T git@github.com
   ```

   Должно появиться: `Hi maksim4351! You've successfully authenticated...`

---

## 🌐 Способ 3: Через веб-интерфейс (БЕЗ GIT)

Если не хотите настраивать git, можно загрузить файлы через браузер.

### Шаги:

1. Откройте: https://github.com/maksim4351/api-health-monitor
2. Нажмите **"Add file"** → **"Upload files"**
3. Перетащите папки `api_monitor/`, `tests/`, `.github/`
4. Введите сообщение коммита: `Add project structure`
5. Нажмите **"Commit changes"**

**Недостаток:** Нужно загружать файлы по одному или небольшими группами.

---

## 📋 Быстрая настройка (для Windows)

### Вариант A: Personal Access Token

1. **Создайте токен:** https://github.com/settings/tokens
2. **Настройте git:**

   ```bash
   git config --global user.name "maksim4351"
   git config --global user.email "ваш@email.com"
   ```

3. **При push используйте токен как пароль**

### Вариант B: GitHub Desktop

1. Скачайте: https://desktop.github.com/
2. Войдите в аккаунт GitHub
3. Откройте репозиторий
4. Добавьте файлы через интерфейс
5. Нажмите "Commit" и "Push"

---

## ✅ Проверка настройки

### Проверить имя и email:

```bash
git config --global user.name
git config --global user.email
```

### Проверить удаленный репозиторий:

```bash
git remote -v
```

Должно показать:
```
origin  https://github.com/maksim4351/api-health-monitor.git (fetch)
origin  https://github.com/maksim4351/api-health-monitor.git (push)
```

---

## 🎯 Рекомендация

**Для начинающих:** Используйте **Способ 1 (Personal Access Token)** или **Способ 3 (веб-интерфейс)**.

**Для постоянной работы:** Настройте **Способ 2 (SSH ключи)**.

---

## ❓ Частые проблемы

### "Permission denied" при push

- Проверьте, что используете Personal Access Token (не пароль)
- Убедитесь, что токен имеет права `repo`

### "Repository not found"

- Проверьте, что репозиторий существует
- Убедитесь, что у вас есть права на запись

### "Authentication failed"

- Проверьте правильность логина и токена
- Создайте новый токен, если старый истек

