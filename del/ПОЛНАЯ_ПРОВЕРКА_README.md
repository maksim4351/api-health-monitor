# ПОЛНАЯ_ПРОВЕРКА.bat - Комплексная проверка проекта

## 📋 Что делает этот скрипт?

Выполняет **полную проверку** всех компонентов проекта API Health Monitor:

### ✅ Проверки:

1. **Проверка Python** - наличие и версия Python
2. **Установка зависимостей** - автоматическая установка из requirements.txt
3. **Проверка синтаксиса** - проверка всех .py файлов на синтаксические ошибки
4. **Проверка импортов** - проверка, что все модули импортируются корректно
5. **Запуск тестов** - выполнение всех unit-тестов с покрытием кода
6. **Проверка конфигурации** - валидация config.yaml
7. **Проверка CLI** - проверка работы командной строки
8. **Проверка веб-сервера** - проверка импорта веб-сервера

### 📊 Итоговый отчет:

После всех проверок выводится итоговый отчет:
- ✅ Все проверки пройдены успешно
- ⚠️ Обнаружены проблемы (с указанием каких)

## 🚀 Как использовать?

### Вариант 1: Двойной клик
Просто дважды кликните на файл `ПОЛНАЯ_ПРОВЕРКА.bat`

### Вариант 2: Из командной строки
```cmd
ПОЛНАЯ_ПРОВЕРКА.bat
```

## 📈 Что проверяется?

### 1. Python
- Проверяет наличие Python 3.8+
- Пробует команды: `python`, `python3`, `py`

### 2. Зависимости
- Устанавливает все пакеты из `requirements.txt`
- Проверяет успешность установки

### 3. Синтаксис
- Проверяет все файлы в `api_monitor/`
- Проверяет все файлы в `tests/`
- Использует `py_compile` для проверки

### 4. Импорты
- Проверяет импорт всех основных модулей:
  - `api_monitor.checker`
  - `api_monitor.cli`
  - `api_monitor.loader`
  - `api_monitor.reporter`
  - `api_monitor.scheduler`
  - `api_monitor.web_server`

### 5. Тесты
- Запускает `pytest` с покрытием кода
- Генерирует HTML отчет в `htmlcov/`
- Показывает результаты в терминале

### 6. Конфигурация
- Проверяет наличие `config.yaml`
- Валидирует структуру конфигурации
- Проверяет корректность данных

### 7. CLI
- Проверяет работу команды `api-monitor --help`
- Убеждается, что CLI запускается

### 8. Веб-сервер
- Проверяет импорт `WebMonitoringServer`
- Проверяет импорт `MonitoringHandler`

## 📊 Отчеты

После проверки создаются:
- **HTML отчет покрытия:** `htmlcov/index.html`
- **Терминальный отчет:** вывод в консоль

## ✅ Пример успешного выполнения

```
========================================
  ИТОГОВЫЙ ОТЧЕТ
========================================

✅ Тесты: ВСЕ ПРОШЛИ
✅ Конфигурация: OK
✅ CLI: РАБОТАЕТ
✅ Веб-сервер: OK

========================================
  ✅ ВСЕ ПРОВЕРКИ ПРОЙДЕНЫ УСПЕШНО!
========================================

Проект готов к использованию!
```

## ⚠️ Пример с ошибками

```
========================================
  ИТОГОВЫЙ ОТЧЕТ
========================================

⚠️ Тесты: НЕКОТОРЫЕ НЕ ПРОШЛИ
✅ Конфигурация: OK
✅ CLI: РАБОТАЕТ
✅ Веб-сервер: OK

========================================
  ⚠️ ОБНАРУЖЕНЫ ПРОБЛЕМЫ
========================================

Проверьте ошибки выше и исправьте их.
```

## 🔧 Когда использовать?

- ✅ Перед коммитом изменений
- ✅ Перед публикацией релиза
- ✅ После больших изменений в коде
- ✅ Для проверки работоспособности проекта
- ✅ При настройке на новом компьютере

## 💡 Советы

1. **Запускайте регулярно** - после значительных изменений
2. **Проверяйте отчеты** - смотрите `htmlcov/index.html` для деталей
3. **Исправляйте ошибки** - не игнорируйте предупреждения
4. **Используйте перед деплоем** - убедитесь, что все работает

## 🚨 Решение проблем

### Python не найден
- Установите Python 3.8+
- Добавьте Python в PATH

### Ошибки синтаксиса
- Проверьте файлы с ошибками
- Исправьте синтаксические ошибки

### Тесты не проходят
- Откройте `htmlcov/index.html` для деталей
- Проверьте логи в консоли
- Исправьте тесты или код

### Конфигурация невалидна
- Проверьте `config.yaml`
- Убедитесь в правильности структуры

---

**Используйте этот скрипт для уверенности, что проект работает корректно! 🎯**

