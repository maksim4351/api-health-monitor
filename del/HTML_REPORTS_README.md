# HTML Отчёты - Инструкция

## 📊 Типы HTML отчётов

### 1. Отчёт результатов проверки API
Красивый HTML отчёт с результатами проверки всех API.

**Генерация:**
```cmd
generate_html_report.bat
```

Или вручную:
```cmd
api-monitor run config.yaml --format html --output api_report.html
```

**Что содержит:**
- ✅ Статистика (всего, успешных, неудачных, процент успешности)
- ✅ Детальная таблица результатов
- ✅ Цветовая индикация статусов
- ✅ Информация о задержках
- ✅ Ссылки на проверяемые API

**Где находится:**
- `api_report.html` в корне проекта

### 2. Отчёт покрытия кода тестами
HTML отчёт о том, какой процент кода покрыт тестами.

**Генерация:**
```cmd
run_tests.bat
```

Или вручную:
```cmd
pytest --cov=api_monitor --cov-report=html
```

**Что содержит:**
- ✅ Общий процент покрытия
- ✅ Покрытие по файлам
- ✅ Детальная информация по строкам кода
- ✅ Непокрытые участки кода

**Где находится:**
- `htmlcov/index.html` в папке `htmlcov/`

## 🚀 Быстрый старт

### Вариант 1: Автоматическая генерация всех отчётов
```cmd
generate_html_report.bat
```

Этот файл:
1. Проверит зависимости
2. Сгенерирует отчёт результатов проверки API
3. Сгенерирует отчёт покрытия кода
4. Предложит открыть отчёты в браузере

### Вариант 2: Открыть существующие отчёты
```cmd
open_html_reports.bat
```

Откроет все доступные HTML отчёты в браузере.

## 📖 Подробная инструкция

### Генерация отчёта результатов проверки API

1. **Убедитесь, что есть config.yaml:**
   ```cmd
   dir config.yaml
   ```

2. **Запустите генерацию:**
   ```cmd
   generate_html_report.bat
   ```

3. **Или вручную:**
   ```cmd
   python -m api_monitor.cli run config.yaml --format html --output api_report.html
   ```

4. **Откройте в браузере:**
   - Дважды кликните на `api_report.html`
   - Или используйте `open_html_reports.bat`

### Генерация отчёта покрытия кода

1. **Установите pytest (если не установлен):**
   ```cmd
   pip install pytest pytest-cov
   ```

2. **Запустите тесты с HTML отчётом:**
   ```cmd
   run_tests.bat
   ```

3. **Или вручную:**
   ```cmd
   pytest --cov=api_monitor --cov-report=html
   ```

4. **Откройте в браузере:**
   - Откройте `htmlcov/index.html`
   - Или используйте `open_html_reports.bat`

## 🎨 Особенности HTML отчётов

### Отчёт результатов проверки API

- **Адаптивный дизайн** - работает на всех устройствах
- **Цветовая индикация:**
  - 🟢 Зелёный - успешные проверки
  - 🔴 Красный - неудачные проверки
  - 🟡 Жёлтый - таймауты
- **Интерактивные элементы:**
  - Кликабельные ссылки на API
  - Подсветка строк при наведении
- **Статистика:**
  - Общее количество проверок
  - Количество успешных/неудачных
  - Процент успешности

### Отчёт покрытия кода

- **Детальная информация:**
  - Покрытие по каждому файлу
  - Покрытие по строкам
  - Непокрытые участки кода
- **Навигация:**
  - Переход между файлами
  - Поиск по коду
- **Визуализация:**
  - Цветовая индикация покрытия
  - Проценты покрытия

## 💡 Советы

1. **Регулярно проверяйте покрытие кода:**
   - Стремитесь к покрытию >80%
   - Используйте отчёт для поиска непокрытых участков

2. **Используйте HTML отчёты для презентаций:**
   - Отчёт результатов API можно показать команде
   - Визуально понятная информация

3. **Сохраняйте отчёты:**
   - HTML отчёты можно сохранить для истории
   - Полезно для отслеживания изменений

4. **Автоматизация:**
   - Добавьте генерацию HTML отчётов в CI/CD
   - Автоматически открывайте отчёты после проверок

## 🔧 Решение проблем

### Отчёт не генерируется
- Проверьте, что установлены все зависимости
- Убедитесь, что config.yaml существует
- Проверьте права на запись в директорию

### Отчёт не открывается в браузере
- Попробуйте открыть файл напрямую
- Проверьте, что файл не повреждён
- Используйте другой браузер

### Отчёт пустой
- Проверьте, что API доступны
- Убедитесь, что config.yaml корректен
- Проверьте логи ошибок

## 📁 Структура файлов

```
api-monitor/
├── api_report.html          # Отчёт результатов проверки API
├── htmlcov/                 # Отчёты покрытия кода
│   ├── index.html          # Главная страница покрытия
│   └── ...                 # Детальные отчёты по файлам
├── generate_html_report.bat # Генерация всех HTML отчётов
└── open_html_reports.bat    # Открытие HTML отчётов
```

## 🎯 Примеры использования

### Ежедневная проверка
```cmd
generate_html_report.bat
```

### Перед коммитом
```cmd
run_tests.bat
:: Проверьте htmlcov/index.html для покрытия кода
```

### Презентация результатов
```cmd
api-monitor run config.yaml --format html --output presentation.html
:: Откройте presentation.html в браузере
```

---

**Приятной работы с HTML отчётами! 📊**

