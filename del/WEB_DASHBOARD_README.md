# Веб-дашборд для мониторинга API

## 🌐 Веб-интерфейс в браузере

Теперь вы можете мониторить API не только в командной строке, но и в красивом веб-интерфейсе!

## 🚀 Быстрый запуск

### Вариант 1: Через bat-файл (САМЫЙ ПРОСТОЙ)

Дважды кликните:
```
run_web_monitoring.bat
```

Браузер откроется автоматически на `http://localhost:8080`

**💡 Важно:** Если порт 8080 занят, система автоматически найдет свободный порт и сообщит вам об этом.

### Вариант 2: Через командную строку

```bash
# С интервалом из config.yaml
api-monitor watch config.yaml --web

# С кастомным интервалом
api-monitor watch config.yaml --web --interval 30

# С кастомным портом
api-monitor watch config.yaml --web --port 9000
```

## 🔧 Решение проблем с портами

### Порт занят?

Если вы видите ошибку `PermissionError: [WinError 10013]`, это означает, что порт занят.

**✅ Автоматическое решение:**
- Система автоматически найдет свободный порт (8081, 8082, и т.д.)
- Вы увидите сообщение: `✅ Найден свободный порт: 8081`
- Браузер откроется на правильном порту

**🔧 Ручное решение:**
1. Укажите другой порт: `--port 9000`
2. Закройте программу, использующую порт 8080
3. Запустите от имени администратора (если нужно)

## 📊 Что показывает веб-дашборд

### Вкладка "Мониторинг"
- ✅ **Статистика в реальном времени:**
  - Всего API
  - Успешных
  - Неудачных
  - Процент успешности

- ✅ **Таблица результатов:**
  - Статус каждого API (цветовая индикация)
  - HTTP коды
  - Задержки
  - Ошибки

- ✅ **Автообновление:** Данные обновляются каждые 5 секунд

### Вкладка "О проекте"
- 📖 Описание проекта
- ✨ Возможности
- 🛠️ Технологии
- 🔗 Ссылки

### Вкладка "Управление API" ⚙️
- ➕ **Добавление новых API:**
  - Форма для добавления API с настройками
  - Метод запроса (GET, POST, PUT, DELETE, PATCH)
  - Таймаут и ожидаемый HTTP статус
  - Автоматическое сохранение в config.yaml

- ⭐ **Популярные API:**
  - Список из 20 популярных API для быстрого добавления
  - Один клик для добавления в мониторинг
  - Google, GitHub, JSONPlaceholder, HTTPBin и другие

- 📋 **Управление текущими API:**
  - Просмотр всех добавленных API
  - Редактирование существующих API
  - Удаление API (с подтверждением)
  - Автоматическое сохранение изменений

### Вкладка "Статистика"
- 📈 Общая статистика мониторинга
- ⏱️ Время работы
- 📊 Количество проверок
- 📅 История

## 🔔 Уведомления об ошибках

- **Визуальные:** Красные алерты при ошибках
- **Браузерные:** Уведомления браузера (если разрешены)
- **Автоматические:** Появляются сразу при обнаружении ошибок

## 🎨 Особенности

- ✅ **Красивый дизайн** — современный UI
- ✅ **Адаптивный** — работает на всех устройствах
- ✅ **В реальном времени** — автообновление данных
- ✅ **Цветовая индикация** — визуальное отображение статусов
- ✅ **История** — сохранение последних 100 проверок
- ✅ **Управление API** — добавление, редактирование, удаление через веб-интерфейс
- ✅ **Популярные API** — быстрый доступ к 20 популярным API
- ✅ **Автосохранение** — все изменения сохраняются в config.yaml

## 🛑 Остановка

Нажмите `Ctrl+C` в командной строке

## 🔧 Настройка

### Изменение порта

```bash
api-monitor watch config.yaml --web --port 9000
```

### Изменение интервала

```bash
api-monitor watch config.yaml --web --interval 30
```

## 📱 Доступ с других устройств

Если вы в локальной сети, можете открыть дашборд с других устройств:

```
http://ВАШ_IP:8080
```

Например:
```
http://192.168.1.100:8080
```

## 💡 Примеры использования

### Мониторинг каждую минуту в браузере
```bash
api-monitor watch config.yaml --web --interval 60
```

### Мониторинг на другом порту
```bash
api-monitor watch config.yaml --web --port 3000
```

### Комбинация: веб + сохранение в файл
```bash
api-monitor watch config.yaml --web --format html --output reports/report.html
```

## 🎯 Два режима работы

### 1. Консольный режим
```bash
api-monitor watch config.yaml
```
- Вывод в командную строку
- Подходит для серверов без GUI

### 2. Веб-режим
```bash
api-monitor watch config.yaml --web
```
- Веб-интерфейс в браузере
- Визуализация и уведомления
- Подходит для локального мониторинга

## 📊 API Endpoints

Веб-сервер предоставляет API:

- `GET /` - Главная страница дашборда
- `GET /api/data` - JSON данные мониторинга
- `GET /api/stats` - Статистика
- `GET /api/project` - Информация о проекте
- `GET /api/apis` - Список текущих API
- `GET /api/popular` - Список популярных API
- `POST /api/apis` - Добавить новый API
- `PUT /api/apis/{name}` - Обновить существующий API
- `DELETE /api/apis/{name}` - Удалить API

## ⚙️ Управление API через веб-интерфейс

### Добавление нового API

1. Откройте вкладку "Управление API"
2. Заполните форму:
   - **Название API** — имя для отображения
   - **URL** — адрес API
   - **Метод** — HTTP метод (GET, POST, PUT, DELETE, PATCH)
   - **Таймаут** — время ожидания в секундах
   - **Ожидаемый статус** — HTTP код успешного ответа
3. Нажмите "Добавить API"
4. API автоматически сохранится в `config.yaml`

### Быстрое добавление популярных API

1. Откройте вкладку "Управление API"
2. В разделе "Популярные API" нажмите на нужный API
3. Данные автоматически заполнятся в форму
4. Нажмите "Добавить API"

### Редактирование API

1. В разделе "Текущие API" нажмите "✏️ Редактировать"
2. Данные загрузятся в форму
3. Измените нужные параметры
4. Нажмите "Обновить API"

### Удаление API

1. В разделе "Текущие API" нажмите "🗑️ Удалить"
2. Подтвердите удаление
3. API будет удален из конфигурации

**⚠️ Важно:** Все изменения автоматически сохраняются в файл `config.yaml`

## 🔍 Решение проблем

### Порт занят
Измените порт:
```bash
api-monitor watch config.yaml --web --port 8081
```

### Браузер не открывается
Откройте вручную: `http://localhost:8080`

### Данные не обновляются
- Проверьте интернет-соединение
- Убедитесь, что API доступны
- Проверьте консоль на ошибки

---

**Наслаждайтесь визуальным мониторингом! 🎉**

