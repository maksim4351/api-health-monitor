# Инструкция по использованию BAT-файлов

## 📋 Созданные файлы

### 1. `check_and_run.bat` - Полная проверка кода ⭐
**Основной файл для проверки всего проекта**

Что делает:
- ✅ Проверяет наличие Python
- ✅ Проверяет наличие pip
- ✅ Устанавливает зависимости
- ✅ Проверяет синтаксис всех Python файлов
- ✅ Запускает тесты (если pytest установлен)
- ✅ Запускает основной функционал для проверки работы

**Использование:**
```cmd
check_and_run.bat
```

### 2. `quick_test.bat` - Быстрый тест
**Быстрая проверка работы проекта**

Что делает:
- ✅ Проверяет Python
- ✅ Устанавливает зависимости (если нужно)
- ✅ Запускает проверку API

**Использование:**
```cmd
quick_test.bat
```

### 3. `run_tests.bat` - Запуск тестов
**Запуск всех unit-тестов**

Что делает:
- ✅ Устанавливает зависимости
- ✅ Устанавливает pytest (если нужно)
- ✅ Запускает все тесты
- ✅ Создаёт отчёт о покрытии кода

**Использование:**
```cmd
run_tests.bat
```

### 4. `install_and_setup.bat` - Установка проекта
**Полная установка проекта в режиме разработки**

Что делает:
- ✅ Обновляет pip
- ✅ Устанавливает все зависимости
- ✅ Устанавливает пакет в режиме разработки
- ✅ Проверяет установку

**Использование:**
```cmd
install_and_setup.bat
```

## 🚀 Рекомендуемый порядок использования

### Первый запуск:
1. Запустите `install_and_setup.bat` - установит всё необходимое
2. Запустите `check_and_run.bat` - проверит весь проект

### Регулярная проверка:
- Используйте `quick_test.bat` для быстрой проверки
- Используйте `run_tests.bat` перед коммитом изменений

## ⚠️ Требования

- Python 3.8 или выше
- Интернет-соединение (для установки зависимостей и проверки API)

## 🔍 Что проверяется

### Синтаксис:
- Все файлы в `api_monitor/`
- Все файлы в `tests/`
- `test_run.py`

### Функциональность:
- Загрузка конфигурации
- Проверка API
- Форматирование отчётов
- CLI интерфейс

### Тесты:
- Тесты загрузки конфигурации
- Тесты проверки API
- Тесты форматирования
- Тесты CLI

## 📊 Результаты

После запуска `check_and_run.bat` вы увидите:
- ✅ Статус проверки Python
- ✅ Статус установки зависимостей
- ✅ Результаты проверки синтаксиса
- ✅ Результаты тестов
- ✅ Результаты работы основного функционала

## 🐛 Решение проблем

### Ошибка: "Python не найден"
**Решение:** Установите Python 3.8+ с https://www.python.org/downloads/
При установке отметьте "Add Python to PATH"

### Ошибка: "pip не найден"
**Решение:** 
```cmd
python -m ensurepip --upgrade
```

### Ошибка: "ModuleNotFoundError"
**Решение:** Запустите `install_and_setup.bat` для установки зависимостей

### Тесты не запускаются
**Решение:** 
```cmd
pip install pytest pytest-cov
```

## 💡 Советы

1. **Перед коммитом** - всегда запускайте `check_and_run.bat`
2. **При изменении кода** - используйте `run_tests.bat` для проверки
3. **Быстрая проверка** - используйте `quick_test.bat`

## 📝 Примечания

- Все bat-файлы используют UTF-8 кодировку для корректного отображения русских символов
- Файлы автоматически проверяют наличие необходимых инструментов
- При ошибках файлы останавливаются с сообщением об ошибке

