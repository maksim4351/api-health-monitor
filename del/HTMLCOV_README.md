# HTML Coverage Reports (htmlcov)

## 📊 Что это?

`htmlcov/` — это папка с HTML-отчетами о **покрытии кода тестами** (code coverage).

## 🎯 Для чего нужен?

Эти отчеты показывают:
- ✅ Какие части кода покрыты тестами
- ❌ Какие части кода не покрыты тестами
- 📈 Процент покрытия кода тестами

## 📁 Файлы в htmlcov/

- `index.html` — главная страница с общей статистикой
- `function_index.html` — индекс всех функций в проекте
- Другие файлы — детальные отчеты по каждому модулю

## 🔧 Как создать отчеты?

Запустите тесты с покрытием:

```bash
# Windows
run_tests.bat

# Или вручную
pytest --cov=api_monitor --cov-report=html
```

## 📖 Как использовать?

1. Откройте `htmlcov/index.html` в браузере
2. Увидите общую статистику покрытия
3. Кликните на любой файл для детального просмотра
4. Зеленым цветом — покрыто тестами
5. Красным цветом — не покрыто тестами

## ⚠️ Важно

- Это **НЕ** часть веб-интерфейса мониторинга API
- Это инструмент для **разработчиков**
- Нужен для проверки качества тестов
- Можно удалить — пересоздастся при следующем запуске тестов

## 🚫 Почему нет в индексе?

`htmlcov/function_index.html` не нужен в основном проекте:
- Это временный файл для разработки
- Генерируется автоматически при тестах
- Обычно добавляется в `.gitignore`

---

**Вывод:** `htmlcov/` — это отчеты о тестах, не связаны с веб-интерфейсом мониторинга API.

