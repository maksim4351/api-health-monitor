# 📚 Как добавить папки на GitHub

## ⚠️ Важно понимать

**На GitHub нельзя создать пустую папку!** Папки создаются автоматически, когда в них добавляются файлы.

Если папки не видны на GitHub, значит файлы из этих папок не были добавлены в git.

---

## 🚀 Способ 1: Через Git команды (РЕКОМЕНДУЕТСЯ)

Это самый правильный и быстрый способ для множества файлов.

### Шаги:

1. **Откройте терминал** в папке проекта:
   ```bash
   cd D:\Proekt\github\api-monitor
   ```

2. **Проверьте статус:**
   ```bash
   git status
   ```
   Вы увидите список файлов, которые не добавлены в git.

3. **Добавьте все папки:**
   ```bash
   git add api_monitor/
   git add tests/
   git add .github/
   git add .
   ```

4. **Проверьте, что добавлено:**
   ```bash
   git status
   ```
   Должны увидеть все файлы в разделе "Changes to be committed".

5. **Создайте коммит:**
   ```bash
   git commit -m "Add project structure: api_monitor, tests, .github"
   ```

6. **Отправьте на GitHub:**
   ```bash
   git push origin main
   ```
   Или если ваша ветка называется `master`:
   ```bash
   git push origin master
   ```

---

## 🌐 Способ 2: Через веб-интерфейс GitHub

### Для одной папки (например, `api_monitor/`):

1. Откройте репозиторий: https://github.com/maksim4351/api-health-monitor

2. Нажмите кнопку **"Add file"** → **"Create new file"**

3. **Для создания папки `api_monitor/`**, в поле имени файла введите:
   ```
   api_monitor/__init__.py
   ```
   GitHub автоматически создаст папку `api_monitor/`!

4. Вставьте содержимое файла `__init__.py` в редактор

5. Прокрутите вниз, введите сообщение коммита:
   ```
   Add api_monitor/__init__.py
   ```

6. Нажмите **"Commit new file"**

7. **Повторите для каждого файла:**
   - `api_monitor/checker.py`
   - `api_monitor/cli.py`
   - `api_monitor/loader.py`
   - и т.д.

### ⚠️ Недостаток:
Этот способ очень долгий, если файлов много (у вас 9 файлов в `api_monitor/`, 6 в `tests/`, и т.д.)

---

## 📤 Способ 3: Drag & Drop (загрузка файлов)

1. Откройте репозиторий на GitHub

2. Нажмите **"Add file"** → **"Upload files"**

3. **Перетащите папки** `api_monitor/`, `tests/`, `.github/` в окно браузера

4. Или нажмите **"choose your files"** и выберите все файлы из папок

5. Введите сообщение коммита:
   ```
   Add project structure
   ```

6. Нажмите **"Commit changes"**

### ⚠️ Внимание:
- Нужно выбрать **ВСЕ файлы** из папок
- Нельзя перетащить пустую папку
- GitHub может ограничивать размер загружаемых файлов

---

## 📋 Список файлов, которые нужно добавить

### 📁 `api_monitor/` (9 файлов):
- `__init__.py`
- `cache.py`
- `checker.py`
- `cli.py`
- `loader.py`
- `notifier.py`
- `reporter.py`
- `scheduler.py`
- `web_server.py`

### 📁 `tests/` (7 файлов):
- `__init__.py`
- `conftest.py`
- `README.md`
- `test_cache.py`
- `test_checker.py`
- `test_cli.py`
- `test_loader.py`
- `test_notifier.py`
- `test_reporter.py`

### 📁 `.github/` (папка с конфигурацией):
- `workflows/test.yml`
- `ISSUE_TEMPLATE/bug_report.md`
- `ISSUE_TEMPLATE/feature_request.md`
- `ISSUE_TEMPLATE/question.md`
- `ISSUE_TEMPLATE/config.yml`
- `RELEASE_TEMPLATE.md`

---

## ✅ Быстрая проверка

После добавления файлов проверьте на GitHub:

1. Откройте: https://github.com/maksim4351/api-health-monitor
2. Должны увидеть папки:
   - 📁 `api_monitor/`
   - 📁 `tests/`
   - 📁 `.github/`
3. Кликните на папку - должны увидеть файлы внутри

---

## 🎯 Рекомендация

**Используйте Способ 1 (Git команды)** - это самый быстрый и правильный способ для множества файлов.

Или просто запустите скрипт `ADD_TO_GIT.bat`, который я создал - он сделает всё автоматически!

